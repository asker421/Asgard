window.AsSources={parse(text){return text.split('\n').map((line,i)=>({line:line.trim(),row:i+1})).filter(x=>x.line&&!x.line.startsWith('#')).map(x=>{const p=x.line.split('|').map(v=>v.trim());return{row:x.row,ok:p.length===8,name:p[0]||'',type:p[1]||'',url:p[2]||'',language:p[3]||'',enabled:p[4]==='true',priority:Number(p[5]||0),auth:p[6]==='true',notes:p[7]||'',error:p.length===8?'':'Line must have 8 columns'}})},preview(text){return this.parse(text).map(x=>x.ok?'OK '+x.row+': '+x.name+' ('+x.type+')':'ERROR '+x.row+': '+x.error).join('\n')}};
