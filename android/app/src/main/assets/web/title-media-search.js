(function(){
  function text(v){return String(v||'').trim()}
  function esc(v){return AsUI.escape(v)}
  function chip(v){return '<span class="chip">'+esc(v)+'</span>'}
  function btn(label,action,cls){return '<button class="btn focusable '+(cls||'')+'" onclick="'+action+'">'+esc(label)+'</button>'}
  function enabledSources(){try{return AsSources.parse(AsStore.readSources()||'').filter(s=>s.ok&&s.enabled)}catch(e){return []}}
  function settings(){try{return AsStore.parserSettings()}catch(e){return {}}}
  function hasConfiguredSearch(){const src=enabledSources();const st=settings();return src.length>0||!!(st.parserUrl||st.jacredBaseUrl)}
  function setupState(){const src=enabledSources();const st=settings();return {enabledSources:src.length,parserUrl:!!(st.parserUrl||st.jacredBaseUrl),serviceUrl:!!st.torrServerUrl,apiKey:!!(st.apiKey||st.jacredApiKey)}}
  function setupPanel(){const s=setupState();return '<div class="panel"><h2>Search setup</h2><p>'+chip('enabled sources '+s.enabledSources)+chip(s.parserUrl?'parser set':'parser missing')+chip(s.serviceUrl?'service set':'service missing')+chip('user-controlled')+'</p><p class="muted">Search uses only your configured sources/parsers. No bundled catalogs are included.</p><div class="source-actions">'+btn('Setup wizard','AsSetupWizard.render()')+btn('Sources','AsUI.nav(\'Источники\')','secondary')+btn('Parser & service','AsParserSettings.render()','secondary')+'</div></div>'}
  function noConfig(out){out.innerHTML=setupPanel()+AsState.error('No configured search input','Add or enable at least one user-controlled source, or configure your parser URL. Asgard cannot search media titles without your configured input.',[{label:'Setup wizard',action:'AsSetupWizard.render()'},{label:'Sources',action:"AsUI.nav('Источники')"},{label:'Parser & service',action:'AsParserSettings.render()'}]);AsInput&&AsInput.refresh&&AsInput.refresh()}
  function quality(r){return r.quality||''}
  function resultUrl(r){return r.magnetUrl||r.torrentUrl||r.url||''}
  function normalizedResults(report,query){if(window.AsSearchNormalize&&AsSearchNormalize.normalizeList)return AsSearchNormalize.normalizeList(report.results||[],query);return (report.results||[]).map((r,i)=>Object.assign({normalizedId:'title_'+i,kind:r.classification||r.kind||'link',sourceName:r.sourceName||r.source||'User source',rightsStatus:r.rightsStatus||'User Source / Unknown Rights'},r))}
  function groups(list){if(window.AsSearchNormalize&&AsSearchNormalize.group)return AsSearchNormalize.group(list);const g={playable:[],torrent:[],magnet:[],link:[],unknown:[]};list.forEach(r=>(g[r.kind]||g.unknown).push(r));return g}
  function card(r,i){return '<div tabindex="0" class="source-row focusable"><h3>'+esc(r.title||'Media result')+'</h3><p>'+chip(r.kind||r.classification||'link')+chip(r.sourceName||'User source')+chip(r.rightsStatus||'User Source / Unknown Rights')+(quality(r)?chip(quality(r)):'')+(r.sizeLabel?chip(r.sizeLabel):'')+(r.seeders!==undefined?chip('S '+r.seeders):'')+(r.peers!==undefined?chip('P '+r.peers):'')+'</p><p class="muted">'+esc(r.description||resultUrl(r))+'</p><p class="muted">'+esc(resultUrl(r))+'</p><div class="source-actions">'+actions(r,i)+'</div></div>'}
  function actions(r,i){let a='';if(r.kind==='playable'||r.classification==='playable')a+=btn('▶ Смотреть','AsMediaSearch.play('+i+')');if(r.kind==='torrent'||r.kind==='magnet'||r.classification==='torrent'||r.classification==='magnet')a+=btn('Create media task','AsMediaSearch.createTask('+i+')')+btn('Prepare stream','AsMediaSearch.playViaService('+i+')','secondary');if(resultUrl(r)&&!a)a+=btn('Open link','AsMediaSearch.open('+i+')','secondary');return a+btn('Diagnostics','AsMediaSearch.diag('+i+')','secondary')}
  function section(title,items,all){if(!items.length)return '';return '<section class="shelf"><div class="shelf-head"><h2>'+esc(title)+'</h2><span>'+items.length+'</span></div>'+items.map(x=>card(x,all.indexOf(x))).join('')+'</section>'}
  function renderResults(out,query,report){const results=normalizedResults(report,query);const g=groups(results);const summary=window.AsSearchNormalize?AsSearchNormalize.summary(results,report.reports||[]):{total:results.length};AsMediaSearch.items=results;AsMediaSearch.last={query,results,groups:g,reports:report.reports||[],summary};const errors=(report.reports||[]).filter(x=>!x.ok).map(x=>({source:x.source,type:x.type,status:x.status,error:x.error}));if(!results.length){out.innerHTML=setupPanel()+AsState.empty('No media results','No result came back from enabled user-configured sources. Try another title or check source/parser diagnostics.',[{label:'Retry',action:'AsMediaSearch.search()'},{label:'Sources',action:"AsUI.nav('Источники')"},{label:'Diagnostics',action:"AsUI.nav('Диагностика')"}]);return;}out.innerHTML=setupPanel()+'<div class="panel"><h2>Movie title search summary</h2><p>'+chip('query '+query)+chip('total '+summary.total)+chip('playable '+(summary.playable||0))+chip('torrent '+(summary.torrent||0))+chip('magnet '+(summary.magnet||0))+chip('link '+(summary.link||0))+chip('errors '+(summary.errors||0))+chip('sources '+(summary.sourceCount||0))+'</p>'+(errors.length?'<pre>'+esc(JSON.stringify(errors,null,2))+'</pre>':'')+'</div>'+section('Direct playable',g.playable||[],results)+section('Torrent files',g.torrent||[],results)+section('Magnet links',g.magnet||[],results)+section('Other links',g.link||[],results)+section('Unknown',g.unknown||[],results)}
  window.AsTitleMediaSearch={
    async search(){const input=document.getElementById('mediaSearchQuery');const query=text(input&&input.value);const out=document.getElementById('mediaSearchResults');if(!out)return;if(!query){AsState.setEmpty(out,'Введите название','Search by movie or series title.',[]);return;}if(!hasConfiguredSearch()){noConfig(out);return;}AsState.setLoading(out,'Searching movie title','Calling enabled user-configured sources and parsers.');try{const report=await AsSources.searchContent(query);renderResults(out,query,report)}catch(e){AsState.setError(out,'Search failed',e.message||String(e),[{label:'Retry',action:'AsMediaSearch.search()'},{label:'Setup wizard',action:'AsSetupWizard.render()'},{label:'Diagnostics',action:"AsUI.nav('Диагностика')"}])}AsInput&&AsInput.refresh&&AsInput.refresh()},
    render(){const src=enabledSources();AsApp.shell('Search','Movie title → user-configured result → media task → ExoPlayer.','<div class="warn"><b>Legal-safe mode:</b> Asgard searches only your configured sources/parsers. Add only sources you have rights to use.</div><div class="search-box"><input class="focusable" id="mediaSearchQuery" placeholder="Movie or series title"><button class="btn focusable" onclick="AsMediaSearch.search()">Search title</button><button class="btn secondary focusable" onclick="AsSetupWizard.render()">Setup wizard</button><button class="btn secondary focusable" onclick="AsUI.nav(\'Источники\')">Sources</button></div>'+setupPanel()+'<div id="mediaSearchResults">'+AsState.empty(src.length?'Введите название':'No configured source','Enter a movie title. If no source is configured, open Setup wizard first.',src.length?[]:[{label:'Setup wizard',action:'AsSetupWizard.render()'}])+'</div>');setTimeout(()=>AsInput&&AsInput.refresh&&AsInput.refresh(),50)}
  };
  function patch(){if(!window.AsApp||!window.AsMediaSearch||!window.AsSources||!window.AsStore){setTimeout(patch,100);return;}if(AsMediaSearch.__titleSearchPatch)return;AsMediaSearch.__titleSearchPatch=true;AsMediaSearch.search=function(){return AsTitleMediaSearch.search()};AsMediaSearch.render=function(){return AsTitleMediaSearch.render()};AsApp.search=function(){return AsTitleMediaSearch.render()};if(AsUI.state&&AsUI.state.screen==='Поиск')AsTitleMediaSearch.render()}
  patch();
})();
