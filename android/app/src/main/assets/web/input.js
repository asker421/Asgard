window.AsInput={items:[],index:0,mode:'remote',refresh(){this.items=[...document.querySelectorAll('.focusable,.btn,input,textarea,select')];this.items.forEach((x,i)=>{if(!x.hasAttribute('tabindex'))x.setAttribute('tabindex','0');x.dataset.focusIndex=i});if(this.items.length&&!document.activeElement?.dataset?.focusIndex){this.index=0;this.items[0].focus()}},setMode(m){this.mode=m;document.body.classList.toggle('touch-mode',m==='touch');document.body.classList.toggle('remote-mode',m==='remote')},move(delta){if(!this.items.length)this.refresh();if(!this.items.length)return;this.index=(this.index+delta+this.items.length)%this.items.length;this.items[this.index].focus();this.items[this.index].scrollIntoView({block:'nearest',inline:'nearest'})},init(){document.addEventListener('touchstart',()=>this.setMode('touch'),{passive:true});document.addEventListener('mousemove',()=>this.setMode('touch'),{passive:true});document.addEventListener('keydown',e=>{this.setMode('remote');if(e.key==='ArrowDown'||e.key==='ArrowRight'){this.move(1);e.preventDefault()}else if(e.key==='ArrowUp'||e.key==='ArrowLeft'){this.move(-1);e.preventDefault()}else if(e.key==='Enter'){document.activeElement?.click?.()}});setTimeout(()=>this.refresh(),200)}};AsInput.init();
