window.AsInput={items:[],index:0,mode:'remote',refresh(){this.items=[...document.querySelectorAll('.focusable,.btn,input,textarea,select')].filter(x=>{const r=x.getBoundingClientRect();return r.width>0&&r.height>0});this.items.forEach((x,i)=>{if(!x.hasAttribute('tabindex'))x.setAttribute('tabindex','0');x.dataset.focusIndex=i});if(this.items.length&&(!document.activeElement||document.activeElement===document.body)){this.index=0;this.items[0].focus()}},setMode(m){this.mode=m;document.body.classList.toggle('touch-mode',m==='touch');document.body.classList.toggle('remote-mode',m==='remote')},init(){document.addEventListener('touchstart',()=>this.setMode('touch'),{passive:true});document.addEventListener('mousemove',()=>this.setMode('touch'),{passive:true});setTimeout(()=>this.refresh(),200)}};AsInput.init();
