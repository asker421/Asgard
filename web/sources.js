window.AsSources={parse(text){return text.split('\n').map((line,i)=>({line:line.trim(),row:i+1})).filter(x=>x.line&&!x.line.startsWith('#')).map(x=>{const p=x.line.split('|').map(v=>v.trim());return{row:x.row,ok:p.length===8,name:p[0]||'',type:p[1]||'',url:p[2]||'',error:p.length===8?'':'Line must have 8 columns'}})},preview(text){return this.parse(text).map(x=>x.ok?'OK '+x.row+': '+x.name+' ('+x.type+')':'ERROR '+x.row+': '+x.error).join('\n')}};
