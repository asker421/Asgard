window.AsApp={demo:[{title:'Big Buck Bunny',year:'2008',rating:'7.8'},{title:'Sintel',year:'2010',rating:'7.4'}],render(){const s=AsUI.state.screen;if(s==='Главная')return this.home();if(s==='Источники')return this.sources();if(s==='Поиск')return this.search();app.innerHTML=AsUI.hero(s,'Preview screen')},home(){app.innerHTML=AsUI.hero('Asgard TV','iPhone/Safari preview')+'<div class="cards">'+this.demo.map(x=>AsUI.card(x.title,x.year+' · '+x.rating)).join('')+'</div>'},search(){app.innerHTML=AsUI.hero('Поиск','Preview search')+'<input id="q"><button class="btn">Искать</button>'},sources(){app.innerHTML=AsUI.hero('Источники','sources.txt preview')+'<textarea id="src" rows="8"></textarea><button class="btn" onclick="out.textContent=AsSources.preview(src.value)">Preview</button><pre id="out"></pre>'}};window.asgardBack=function(){AsUI.nav('Главная')};AsUI.renderMenu();AsApp.render();
